# Security policy

Report vulnerabilities privately to the maintainers. Do not include tenant
data, credentials, signing keys, or live endpoints in an issue.

The operator accepts immutable digest bindings and public trust references. It
does not store signing private keys, provision infrastructure, or contact an
external service by default. `OP-001` performs no tenant workload mutation.
