# Contributing

Changes require one task packet, one `codex/<packet-id>-<slug>` branch, and one
pull request. Run the exact hash-pinned packet through the trusted offline
launcher. Do not fetch tools or dependencies during verification, access a
warm-source checkout, or broaden evidence claims beyond the packet.
